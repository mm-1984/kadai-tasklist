<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
  <%= render 'tasks/tasks' , tasks: @tasks %>
  <div><%= link_to '新規タスクを作成', new_task_path %></div>
<% else %>
  <div><%= link_to 'Sign up now!', signup_path %></div>
  <div><%= link_to 'Login', login_path %></div>
<% end %>